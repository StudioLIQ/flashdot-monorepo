{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "module": "CommonJS",
    "moduleResolution": "node",
    "outDir": "./dist",
    "rootDir": "."
  },
  "include": [
    "hardhat.config.ts",
    "scripts/**/*.ts",
    "test/hardhat/**/*.ts"
  ],
  "exclude": ["node_modules", "artifacts", "cache", "out", "typechain-types"]
}
